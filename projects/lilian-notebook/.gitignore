# Generated by render/build.mjs — never the deliverable, and never committed.
# notes/*.md is the source of truth; both files below are a ~750KB view of it (the brand
# fonts are embedded), and two sessions rebuilding them would collide the way the Hub's
# index.html did. Rebuild on demand; publish scratch/notebook.artifact.html as the Artifact.
notebook.html
scratch/
