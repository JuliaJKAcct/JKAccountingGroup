# Local build artifacts — regenerated by build-hub.mjs, never the deliverable.
scratch/

# The generated Hub page (~5MB). It used to be committed, but two parallel sessions
# both regenerate it and collided — a stale rebuild silently reverted the other's Hub
# features (e.g. the Legal/Tax filters vs PR #91). The SOURCE (build-hub.mjs) is the
# single source of truth; rebuild on demand and publish as an Artifact. Never commit it.
index.html
